export type Frequency="DAILY"|"WEEKLY"|"MONTHLY"|"YEARLY"|"CUSTOM";
export function advanceRecurrence(value:Date,frequency:Frequency,interval:number):Date{const next=new Date(value);switch(frequency){case'DAILY':next.setUTCDate(next.getUTCDate()+interval);break;case'WEEKLY':next.setUTCDate(next.getUTCDate()+7*interval);break;case'MONTHLY':{const day=next.getUTCDate();next.setUTCDate(1);next.setUTCMonth(next.getUTCMonth()+interval);const last=new Date(Date.UTC(next.getUTCFullYear(),next.getUTCMonth()+1,0)).getUTCDate();next.setUTCDate(Math.min(day,last));break;}case'YEARLY':{const month=next.getUTCMonth(),day=next.getUTCDate();next.setUTCDate(1);next.setUTCFullYear(next.getUTCFullYear()+interval);next.setUTCMonth(month);const last=new Date(Date.UTC(next.getUTCFullYear(),month+1,0)).getUTCDate();next.setUTCDate(Math.min(day,last));break;}case'CUSTOM':next.setUTCDate(next.getUTCDate()+interval);break;}return next;}

