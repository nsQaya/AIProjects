-- Development data is intentionally user-specific and created through the API.
-- Keeping this seed non-financial prevents production-like credentials or ledger drift.
SELECT now() AS seed_checked_at;

